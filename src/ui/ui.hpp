#pragma once

/// Main entry point for the terminal UI.
/// Loads the FTXUI screen, builds the full application tree, and starts the
/// event loop. Returns 0 on clean exit.
namespace ui {

int run();

} // namespace ui
